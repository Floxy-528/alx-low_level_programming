#ifndef MACROLIKE_H
#define MACROLIKE_H
/**
 * file: 3-function_like_macro
 *
 * @x: imput value (an int)
 * Desc: header file that compute absolute value
 */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif

